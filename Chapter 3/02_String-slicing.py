name = "Ikrash Haroon"
str_len = len(name) # Returns the length of the string
print(f"Length of the string is : {str_len}")

# Now I want to show you that how we can get a short string of a particular string.
user = "Ali Haidars"
short_name = user[4:8] # Slicing the string from index 4 to index 8 (exclusive)
print(f"Short name is : {short_name}")
































